[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs", "yarn"]

[build.nixpacksPlan.phases.install]
cmds = ["yarn install"]

[build.nixpacksPlan.phases.build]
cmds = ["yarn build:admin"]

[build.nixpacksPlan.phases.start]
cmds = ["yarn start:admin"]
